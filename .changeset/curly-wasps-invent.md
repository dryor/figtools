---
"@figtools/core": minor
---

Add `svgCode` to `RawFigmaNode`/`FigmaNode`, `flow`/`widthSizing`/`heightSizing`/`strokeSide` to `CommonStyles`, and named `Position`/`Size` types (replacing inline `{x, y}`/`{width, height}`). Read `flow`, sizing modes, and stroke-side from the inspection panel's Width/Height and Border labels.

Rename the exported `FigmaGateway` type to `FigmaNodeSource` — update imports accordingly.
