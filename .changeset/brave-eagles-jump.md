---
"@figtools/cli": minor
---

Write each node's captured SVG to a `.svg` file when present, and write markdown output to `slug/index.md` instead of `slug.md` so every node gets its own directory. Add an `--image-format` flag (`png` by default, `svg`) to choose which format gets captured.
